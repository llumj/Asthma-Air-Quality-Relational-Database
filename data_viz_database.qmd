---
title: "Data Viz for Database"
format: html
editor: visual
---

```{r}
library(tidyverse)
library(DBI)         # low-level database interface, comparable to what we saw in Python
library(dbplyr)
library(duckdb)   
```

Load the Database:

```{r}
getwd()

conn <- DBI::dbConnect(duckdb::duckdb(), dbdir = "asthma_airquality.db")  # adapt the path as needed
```


